import 'package:flutter/material.dart';

class FinanzasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Página de Finanzas'),
      ),
      body: Center(
        child: Text('Bienvenido, Departamento de Finanzas'),
      ),
    );
  }
}
